SELECT u.*, b.cnt FROM spring_users AS u,
(SELECT writer, COUNT(*) AS cnt FROM spring_boards GROUP BY writer) AS b
WHERE b.writer = u.userid AND userid = 'asd@asd'



SELECT COUNT(*) FROM spring_users a, spring_boards b WHERE a.userid = b.writer


<!-- 첨부파일 쿼리 -->
CREATE TABLE BOARD_FILE (
    FNO NUMBER not null,
    BNO NUMBER,
    ORIFIL VARCHAR2(100),
    CONSTRAINT BOARD_FILE_PK PRIMARY KEY (FNO)
);

COMMIT;

ALTER TABLE BOARD_FILE ADD CONSTRAINT FK_BOARD_FILE FOREIGN KEY (BNO) REFERENCE SPRING_BOARDS (ID);


CREATE SEQUENCE SEQ_BOARD_FILE START WITH 1 INCREMENT BY 1;
COMMIT;

SELECT * FROM BOARD_FILE;
INSERT INTO BOARD_FILE VALUES (SEQ_FILE.NEXTVAL, 535, 'temp2.zip');
INSERT INTO BOARD_FILE VALUES (SEQ_FILE.NEXTVAL, 535, 'temp3.zip');
INSERT INTO BOARD_FILE VALUES (SEQ_FILE.NEXTVAL, 535, 'temp4.zip');
INSERT INTO BOARD_FILE VALUES (SEQ_FILE.NEXTVAL, 535, 'temp5.zip');
commit;

SELECT * FROM SPRING_BOARDS A, BOARD_FILE B WHERE A.id = B.bno;

SELECT * FROM USER_SEQUENCES;

// /****************************** procedure query ********************************

create or replace PROCEDURE NOTICE_TB_DATA_PROC 
(   
    P_ID IN NUMBER,
    P_TITLE IN VARCHAR2,
    P_CONTENT IN VARCHAR2,
    P_WRITER IN VARCHAR2,
    P_WRITEDATE IN VARCHAR2,
    P_FILENAME IN VARCHAR2
)
IS 
BEGIN
INSERT INTO NOTICE_TB_DATA (ID, TITLE, CONTENT, WRITER, WRITEDATE, FILENAME)
VALUES (P_ID, P_TITLE, P_CONTENT, P_WRITER, P_WRITEDATE, P_FILENAME);

END NOTICE_TB_DATA_PROC;



